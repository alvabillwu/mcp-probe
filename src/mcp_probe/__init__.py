"""mcp-probe — probe and inspect any MCP server from the CLI.

A minimal, dependency-free MCP (Model Context Protocol) client that speaks
JSON-RPC 2.0 over stdio, letting you discover and invoke the tools,
resources, and prompts exposed by any MCP server.
"""

__version__ = "0.1.0"
